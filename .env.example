# --- API ---
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# --- Postgres (TypeORM) ---
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=meal_ai_agent

# TypeORM behavior: disable synchronize in production, use migrations instead
DB_SYNCHRONIZE=true
DB_LOGGING=true
