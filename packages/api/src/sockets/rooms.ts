export const userRoom = (userId: number): string => `user:${userId}`;
export const familyRoom = (familyId: number): string => `family:${familyId}`
export const threadRoom = (threadId: number): string => `thread:${threadId}`
