export enum ChatMessageRole {
  User = "user",
  Assistant = "assistant",
  System = "system",
}

export enum ChatThreadStatus {
  Active = "active",
  Archived = "archived",
}

export enum ChatThreadScope {
  User = "user",
  Family = "family",
}
